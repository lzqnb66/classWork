# 健康管理系统 - 多项目架构

## 📁 项目结构概述

这是一个完整的健康管理系统，包含三个独立的子项目：

```
健康管理系统/
├── server/          # 后端API服务 (Node.js + TypeScript + Express)
├── admin-app/       # 管理后台前端 (Vue.js + Vite)
├── mobile-app/      # 移动端应用 (Vue.js + Vite)
└── README.md        # 本项目说明文件
```

## 🚀 各项目说明

### 1. 📊 server - 后端API服务
- **技术栈**: Node.js + Express + TypeScript + TypeORM + MySQL
- **功能**: 提供RESTful API接口，用户认证，数据存储和管理
- **端口**: 3000
- **启动**: `cd server && npm run dev`

### 2. 🖥️ admin-app - 管理后台
- **技术栈**: Vue.js 3 + Vite + Vue Router
- **功能**: 系统管理界面，用户管理，数据统计和报表
- **端口**: 5173 (默认)
- **启动**: `cd admin-app && npm run dev`

### 3. 📱 mobile-app - 移动端应用
- **技术栈**: Vue.js 3 + Vite + Vue Router
- **功能**: 用户健康数据记录，步数统计，饮食记录，睡眠跟踪
- **端口**: 5173 (或其他可用端口)
- **启动**: `cd mobile-app && npm run dev`

## 🛠️ 开发环境配置

### 前置要求
- Node.js 16+ 
- MySQL 8.0+
- Git

### 环境设置
1. 克隆项目到本地
2. 为每个子项目安装依赖：
   ```bash
   cd server && npm install
   cd ../admin-app && npm install  
   cd ../mobile-app && npm install
   ```
3. 配置数据库连接（编辑各项目的.env文件）
4. 启动开发服务器

## 🔧 统一开发命令

### 启动所有服务
```bash
# 终端1 - 启动后端
cd server && npm run dev

# 终端2 - 启动管理后台  
cd admin-app && npm run dev

# 终端3 - 启动移动端
cd mobile-app && npm run dev
```

### 构建生产版本
```bash
# 构建后端
cd server && npm run build

# 构建管理后台
cd admin-app && npm run build

# 构建移动端  
cd mobile-app && npm run build
```

## 📋 环境变量配置

各项目都有自己的`.env`文件，请根据实际环境进行配置：

### server/.env
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=health_app
JWT_SECRET=your_jwt_secret
PORT=3000
```

### admin-app/.env 和 mobile-app/.env
```env
VITE_API_BASE_URL=http://localhost:3000/api
```

## 🗂️ Git 管理策略

- 每个子项目都有自己的`.gitignore`文件
- 推荐使用Git子模块或单一仓库管理
- 保持各项目的独立性，避免交叉依赖
- 统一的commit message规范

## 📊 数据库设计

系统使用MySQL数据库，主要包含以下表：
- `users` - 用户信息表
- `records` - 健康记录表
- （其他业务相关表）

## 🚀 部署说明

### 生产环境部署
1. 构建各项目生产版本
2. 配置生产环境变量
3. 设置Nginx反向代理
4. 配置PM2进程管理（后端）
5. 部署静态资源（前端）

### Docker 部署（可选）
```bash
# 构建和运行所有服务
docker-compose up -d
```

## 📞 开发支持

如有问题请参考：
1. 各项目内的README文件
2. 查看API文档（启动后访问 `/api/docs`）
3. 检查控制台错误信息

---

**健康管理系统** © 2024 - 多项目协同开发架构